javaScript
    javascript is used for interactivity

internal javascript
    it is written in the  HTML and inside the script tag
    in the body 

external javaScript
    we use a .js file and access it as src in the script tag
    we put script tag just above the body closing tag

variable
    var is used to make variable
    we can store a number or a string 
    let and const are performance based variables